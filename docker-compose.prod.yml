services:
  a-site:
    build:
      context: .
      dockerfile: Dockerfile
    image: a-site:latest
    ports:
      - "8080:8080"
    env_file:
      - .env
    volumes:
      - ./cache:/app/cache
    restart: unless-stopped

volumes: {}
